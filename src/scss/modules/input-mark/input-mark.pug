mixin input-mark(mark)
  - const {id, text, description} = mark;
  - let fontWeigth = '';
  - if ( description ) {
  -  let fontWeigth = 'thick';
  - }

  .input-mark
    +input-checkbox({
      id: id,
      mod: {visible: "hidden"},
    })
    +label({
      forId: id,
      text: text,
      mod: {checked: "checked", weigth: fontWeigth},
    })
    if description
      .input-mark__description=description
